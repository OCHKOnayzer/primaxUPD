export enum ButtonVariant {
  default = "default",
  primary = "primary",
}
export enum ButtonType {
  button = "button",
  submit = "submit",
  reset = "reset",
}
export enum ButtonSize {
  sm = "sm",
  md = "md",
  lg = "lg",
}
